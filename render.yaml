services:
  - type: web
    name: eastc-ai-assistant
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install
      playwright install-deps
    startCommand: gunicorn app:app